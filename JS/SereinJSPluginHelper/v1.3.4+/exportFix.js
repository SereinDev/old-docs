/**
 * 导出
 * @param {String} key 导出键名
 * @param {*} obj 导出的对象
 */
serein.export = (key, obj) => { };

/**
 * 导入
 * @param {String} key 导入键名
 * @returns {any | null | undefined} 导入对象
 */
serein.import = (key) => Object